version: '3.8'

services:
  bartels-pump:
    build: .
    container_name: bartels-micropump
    privileged: true
    volumes:
      - /dev:/dev
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: python3 test_pump_docker.py
