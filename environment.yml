name: pump-ctrl
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pyserial
  - pip
  - pip:
    - python-dotenv
    - pyyaml
    - usbx
    - pyusb
  # Optional Windows-specific packages (only install if on Windows)
  # - pywin32  # For Windows registry access
